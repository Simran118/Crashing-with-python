import os
# Telling Python Which operating system we are Using
def main():
    # naming the function main
    i=0
# Assuming files in the folder start with 0 by default
    path="___"  
    # link to file Directory
    for filename in os.lisdir(path):
        # Sorting file as a list while looping through files
        my_dest = "__" + str(i) +  ".jpg"
        #  Blank space is what you want your file to be named and JPG is the format i choose for saving the file
        my_source = path + fileaname
        my_dest = path + my_dest
        os.rename(my_source, my_dest)
         i= I + 1
         # Looping the Value by one

if_name_ = '_main_':
    main()
